#include "./roomba.h"
#include <iostream>
#include <fstream>

using namespace std;

floor create_floor(const char* filename)
{
	floor temp;
	//populate floor from fstream using getline into each second level vector
	//
}

/*template formatting*/room::room(const char* filename//appropriate paraemters)
{
	f = create_floor(filename);
}