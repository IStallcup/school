#ifndef GRADEHEAD_H
#define GRADEHEAD_H

std::string in_str;

bool is_test, is_quiz, is_lab, is_assign, is_final;

int num_test, num_quiz, num_lab, num_assign, check_skip_var;

float w_test, w_quiz, w_lab, w_assign, w_final, w_sum = 0;

float a_test = 0, a_quiz, a_lab, a_assign, a_final;

float grade_final;

#endif
